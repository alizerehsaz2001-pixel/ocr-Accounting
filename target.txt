                    {/* Main Area: Simple Settings Layout */}
                    <div className="flex flex-col gap-6 mt-4 pb-2">
                      <div className="flex flex-col gap-5 animate-fadeIn text-right" dir="rtl">
                        {/* Model Selector */}
                        <div className="flex flex-col gap-2">
                          <label className={`text-[11.5px] font-black flex items-center gap-1.5 ${isDarkMode ? "text-slate-200" : "text-slate-800"}`}>
                            <Cpu className="w-4 h-4 text-blue-500" />
                            ۱. مغز پردازشگر هوش مصنوعی (مدل):
                          </label>
                          <div className="grid grid-cols-1 md:grid-cols-2 gap-2.5">:bg-blue-500 text-white shadow-sm"
                                   } ${isDarkMode && ((!preExtractInput.trim() && preExtractFiles.length === 0) || isPreExtractChatLoading) ? "bg-slate-800 text-slate-500" : !isDarkMode && ((!preExtractInput.trim() && preExtractFiles.length === 0) || isPreExtractChatLoading) ? "bg-slate-200 text-slate-400" : ""}`}
                                 >
                                     <Send className={`w-3.5 h-3.5 rotate-180 ${((!preExtractInput.trim() && preExtractFiles.length === 0) || isPreExtractChatLoading) ? "" : "text-white"}`} />
                                 </button>
                               </div>
                               
                               <div className="px-2 pb-0.5 text-right">
                                  <label className="flex items-center gap-1.5 cursor-pointer group justify-end">
                                    <input 
                                      type="checkbox"
                                      checked={customPrompt !== ""}
                                      onChange={(e) => setCustomPrompt(e.target.checked ? "لطفا طبق توافق در پیام‌های قبلی، استخراج را انجام بده و مواردی که با هم بررسی کردیم را اعمال کن." : "")}
                                      className="rounded border-slate-300 text-blue-600 focus:ring-blue-500/30"
                                    />
                                    <span className={`text-[9.5px] font-bold ${isDarkMode ? "text-slate-400 group-hover:text-slate-300" : "text-slate-500 group-hover:text-slate-700"}`}>
                                      اعمال گفتگو به عنوان دستورالعمل استخراج نهایی
                                    </span>
                                  </label>
                               </div>
                            </div>
                          </div>

                          {/* Quick presets list */}
                          <div className="flex flex-col gap-1 text-right" dir="rtl">
                            <span className={`text-[9px] font-black ${isDarkMode ? "text-slate-400" : "text-slate-500"}`}>
                              بارگذاری سریع سناریوهای آماده:
                            </span>
                            <div className="flex flex-wrap gap-1">
                              {[
                                { label: "🧾 فاکتور خرید", text: "این یک فاکتور خرید رسمی است؛ اقلام فاکتور, تعداد، مبلغ واحد و تخفیف‌ها را استخراج کن." },
                                { label: "💳 رسید بانکی", text: "این یک رسید پرداخت مالی است؛ شماره پیگیری، تاریخ، بانک مبدا/مقصد و مبلغ کل را تفکیک کن." },
                                { label: "✍️ سند دست‌نویس", text: "این یک برگه حسابداری دست‌نویس است； با دقت بالا فیلدهای عددی ناخوانا را پردازش کن." },
                                { label: "🌐 صورتحساب ارزی", text: "این فاکتور دارای مبادلات ارزی است؛ ستون نوع ارز و مبالغ معادل را بر اساس واحد ارز ثبت کن." }
                              ].map((preset, idx) => (
                                <button
                                  key={idx}
                                  type="button"
                                  onClick={() => {
                                    setPreExtractInput(preset.text);
                                    showNotification(`سناریو «${preset.label}» لود شد. با دکمه ارسال می‌توانید مکالمه را شروع کنید.`, "info");
                                  }}
                                  className={`text-[8.5px] px-1.5 py-0.5 rounded-lg border transition-all duration-200 hover:-translate-y-0.5 cursor-pointer ${
                                    isDarkMode
                                      ? "bg-slate-900 border-slate-800 hover:border-blue-500/50 text-slate-300"
                                      : "bg-slate-50 border-slate-200 hover:border-blue-300 hover:bg-blue-50/30 text-slate-600"
                                  }`}
                                >
                                  {preset.label}
                                </button>
                              ))}
                            </div>
                          </div>
                        </div>
                      </div>

                      {/* Right Column: Processing Controls, Settings & Guides */}
                      <div className="lg:col-span-7 flex flex-col gap-4 border-r border-slate-100 dark:border-slate-800/60 pr-0 lg:pr-6" dir="rtl">
                        {/* Control Center Tab Switcher */}
                        <div className="flex border-b border-slate-100 dark:border-slate-800/80 pb-1" dir="rtl">
                          {[
                            { id: 'settings', label: '⚙️ تنظیمات پردازش و ممیزی', desc: 'مدل، ماژول و دقت' },
                            { id: 'guide', label: '📖 راهنمای تصویری و عکاسی', desc: 'چک‌لیست و سوالات متداول' }
                          ].map((tab) => {
                            const isSelected = extractionControlTab === tab.id;
                            return (
                              <button
                                key={tab.id}
                                type="button"
                                onClick={() => setExtractionControlTab(tab.id as any)}
                                className="flex-1 pb-3 text-center transition-all relative cursor-pointer"
                              >
                                <span className={`text-[12px] font-black block ${
                                  isSelected 
                                    ? "text-blue-600 dark:text-blue-400" 
                                    : isDarkMode ? "text-slate-400 hover:text-slate-300" : "text-slate-500 hover:text-slate-700"
                                }`}>
                                  {tab.label}
                                </span>
                                <span className="text-[9px] text-slate-400 dark:text-slate-500 block mt-0.5">
                                  {tab.desc}
                                </span>
                                {isSelected && (
                                  <motion.div 
                                    layoutId="extractionControlTabUnderline"
                                    className="absolute bottom-0 left-0 right-0 h-0.5 bg-blue-600 dark:bg-blue-500" 
                                  />
                                )}
                              </button>
                            );
                          })}
                        </div>

                        {/* Tab 1: Settings */}
                      {extractionControlTab === 'settings' && (
                        <div className="flex flex-col gap-5 animate-fadeIn text-right" dir="rtl">
                          {/* Warm Onboarding Alert */}
                          <div className={`p-3.5 rounded-2xl border text-right transition-all ${
                            isDarkMode ? "bg-blue-950/20 border-blue-900/50" : "bg-blue-50/50 border-blue-100"
                          }`}>
                            <div className="flex items-start gap-2.5">
                              <Sparkles className="w-5 h-5 text-amber-500 shrink-0 mt-0.5 animate-pulse" />
                              <div className="min-w-0">
                                <h4 className={`text-[12px] font-bold mb-1 ${isDarkMode ? "text-blue-300" : "text-blue-900"}`}>
                                  🎯 راهنمای گام‌به‌گام برای شروع سریع و آسان:
                                </h4>
                                <ul className={`text-[10px] space-y-1 leading-relaxed list-decimal list-inside ${isDarkMode ? "text-slate-300" : "text-slate-600"}`}>
                                  <li><strong>مغز پردازشگر (مدل)</strong> را متناسب با خوانایی سند خود انتخاب کنید.</li>
                                  <li><strong>ماژول مقصد</strong> را تعیین کنید تا مشخص شود اطلاعات به کجا ارسال شوند.</li>
                                  <li>دکمه سبز رنگ <strong>«شروع استخراج هوشمند»</strong> را در بالا بفشارید!</li>
                                </ul>
                              </div>
                            </div>
                          </div>

                          {/* Model Selector */}
                          <div className="flex flex-col gap-2">
                            <label className={`text-[11.5px] font-black flex items-center gap-1.5 ${isDarkMode ? "text-slate-200" : "text-slate-800"}`}>
                              <Cpu className="w-4 h-4 text-blue-500" />
                              ۱. مغز پردازشگر هوش مصنوعی (مدل):
                            </label>
                            <p className={`text-[9.5px] leading-relaxed mb-1 ${isDarkMode ? "text-slate-400" : "text-slate-500"}`}>
                              هوش مصنوعی را متناسب با میزان وضوح سند یا فاکتور مالی خود انتخاب کنید:
                            </p>
                            <div className="grid grid-cols-2 gap-2.5">
                              {[
                                {
                                  id: "gemini-3.5-flash",
                                  name: "Gemini 3.5 Flash",
                                  badge: "⚡ بسیار سریع و هوشمند",
                                  desc: "پیشنهادی برای ۹۵٪ اسناد چاپی و رسیدهای خوانا.",
                                  recommended: true,
                                  icon: Sparkles,
                                  iconColor: "text-amber-500"
                                },
                                {
                                  id: "gemini-3.1-pro-preview",
                                  name: "Gemini 3.1 Pro",
                                  badge: "🧠 حسابرس ارشد فوق‌دقیق",
                                  desc: "ویژه دست‌نویس‌های مخدوش، کم‌نور یا درهم.",
                                  recommended: false,
                                  icon: Cpu,
                                  iconColor: "text-purple-500"
                                }
                              ].map((modelOpt) => {
                                const isSelected = selectedModel === modelOpt.id;
                                const Icon = modelOpt.icon;
                                return (
                                  <button
                                    key={modelOpt.id}
                                    type="button"
                                    onClick={() => {
                                      setSelectedModel(modelOpt.id);
                                      showNotification(`مدل پردازشگر به «${modelOpt.name}» تغییر یافت.`, "success");
                                    }}
                                    className={`relative flex flex-col items-start justify-between p-3 rounded-2xl border text-right transition-all duration-300 hover:-translate-y-0.5 cursor-pointer ${
                                      isSelected
                                        ? isDarkMode
                                          ? "bg-blue-500/10 border-blue-500/80 text-blue-400 shadow-[0_4px_16px_rgba(59,130,246,0.1)]"
                                          : "bg-blue-50/70 border-blue-500 text-blue-700 shadow-[0_4px_16px_rgba(59,130,246,0.06)]"
                                        : isDarkMode
                                          ? "bg-slate-950/40 border-slate-800 text-slate-400 hover:bg-slate-900 hover:text-slate-300"
                                          : "bg-slate-50 border-slate-200/80 text-slate-500 hover:bg-slate-100 hover:text-slate-800"
                                    }`}
                                  >
                                    {modelOpt.recommended && (
                                      <span className="absolute -top-2 left-3 bg-amber-500 text-white text-[7.5px] px-1.5 py-0.5 rounded-full font-bold animate-pulse">
                                        ✨ پیشنهادی سیستم
                                      </span>
                                    )}
                                    <div className="flex items-center gap-2 mb-2 w-full justify-between">
                                      <div className="flex items-center gap-1.5">
                                        <div className={`p-1.5 rounded-xl ${
                                          isSelected 
                                            ? isDarkMode ? "bg-blue-500/20" : "bg-blue-100/50"
                                            : isDarkMode ? "bg-slate-900" : "bg-slate-200/40"
                                        }`}>
                                          <Icon className={`w-3.5 h-3.5 ${isSelected ? modelOpt.iconColor : "text-slate-400"}`} />
                                        </div>
                                        <span className={`text-[11px] font-extrabold ${isSelected ? (isDarkMode ? "text-slate-100" : "text-slate-900") : ""}`}>
                                          {modelOpt.name}
                                        </span>
                                      </div>
                                      <div className={`w-4 h-4 rounded-full border flex items-center justify-center shrink-0 ${
                                        isSelected 
                                          ? "border-blue-500 bg-blue-500 text-white" 
                                          : isDarkMode ? "border-slate-800" : "border-slate-300"
                                      }`}>
                                        {isSelected && <Check className="w-2.5 h-2.5 stroke-[3]" />}
                                      </div>
                                    </div>
                                    <p className={`text-[9.5px] font-bold mb-1 ${isSelected ? (isDarkMode ? "text-slate-200" : "text-blue-800") : "text-slate-500"}`}>
                                      {modelOpt.badge}
                                    </p>
                                    <p className={`text-[8.5px] leading-normal opacity-90 ${isSelected ? (isDarkMode ? "text-slate-300" : "text-slate-600") : "text-slate-400"}`}>
                                      {modelOpt.desc}
                                    </p>
                                  </button>
                                );
                              })}
                            </div>
                          </div>

                          {/* Target ERP Module */}
                          <div className="flex flex-col gap-2">
                            <label className={`text-[11.5px] font-black flex items-center gap-1.5 ${isDarkMode ? "text-slate-200" : "text-slate-800"}`}>
                              <Folder className="w-4 h-4 text-emerald-500" />
                              ۲. مشخص کردن محل ثبت در نرم‌افزار مالی (ماژول مقصد):
                            </label>
                            <p className={`text-[9.5px] leading-relaxed ${isDarkMode ? "text-slate-400" : "text-slate-500"}`}>
                              تعیین می‌کند اطلاعات استخراج شده دقیقاً به کدام ماژول حسابداری یا دفتر مالی ارسال شود:
                            </p>
                            <div className="relative">
                              <select
                                value={erpDestinationModule}
                                onChange={(e) => {
                                  setErpDestinationModule(e.target.value);
                                  showNotification(`ماژول مقصد به «${e.target.options[e.target.selectedIndex].text}» تغییر یافت.`, "info");
                                }}
                                className={`w-full p-3 rounded-2xl border text-[11px] outline-none text-right appearance-none cursor-pointer font-bold ${
                                  isDarkMode 
                                    ? "bg-slate-950/60 border-slate-800 text-slate-200 focus:border-blue-500" 
                                    : "bg-slate-50 border-slate-200 text-slate-800 focus:border-blue-500"
                                }`}
                              >
                                <option value="general-ledger">📁 دفتر روزنامه و اسناد عمومی (General Ledger)</option>
                                <option value="sales-purchase">🛒 فاکتور خرید و فروش رسمی (Sales/Purchase Invoices)</option>
                                <option value="petty-cash">💳 عملیات تنخواه گردانی و هزینه‌ها (Petty Cash)</option>
                                <option value="payroll">👥 لیست حقوق و دستمزد پرسنل (Payroll & Wages)</option>
                                <option value="inventory">📦 رسید انبار و کاردکس کالا (Inventory & Warehouse)</option>
                              </select>
                              <div className="absolute left-3 top-1/2 -translate-y-1/2 pointer-events-none text-slate-400">
                                <ArrowDown className="w-4 h-4" />
                              </div>
                            </div>
                          </div>

                          {/* Strictness & Accuracy Level */}
                          <div className="flex flex-col gap-2">
                            <label className={`text-[11.5px] font-black flex items-center gap-1.5 ${isDarkMode ? "text-slate-200" : "text-slate-800"}`}>
                              <Scale className="w-4 h-4 text-indigo-500" />
                              ۳. سطح ممیزی مالی و دقت محاسباتی (Accuracy Mode):
                            </label>
                            <p className={`text-[9.5px] leading-relaxed ${isDarkMode ? "text-slate-400" : "text-slate-500"}`}>
                              میزان سخت‌گیری هوش مصنوعی در تراز کردن مبالغ و موازنه دوطرفه را تعیین کنید:
                            </p>
                            <div className="grid grid-cols-3 gap-2">
                              {[
                                { id: 'speed', label: '⚡ سرعت فوق‌العاده', desc: 'تک‌مرحله‌ای و آنی', badge: 'مصرف بهینه', res: 'super-eco', dual: false },
                                { id: 'balanced', label: '🎯 متوازن و استاندارد', desc: 'موازنه حسابداری', badge: 'دقت عالی', res: 'balanced', dual: true },
                                { id: 'audit', label: '🔍 ممیزی فوق‌دقیق', desc: 'اصلاح خطا و ارقام', badge: 'مرحله مکرر ✨', res: 'high', dual: true }
                              ].map((mode) => {
                                const isSel = strictnessMode === mode.id;
                                return (
                                  <button
                                    key={mode.id}
                                    type="button"
                                    onClick={() => {
                                      setStrictnessMode(mode.id as any);
                                      setTokenSettings(prev => ({
                                        ...prev,
                                        imageResolution: mode.res as any,
                                        highAccuracyDualPass: mode.dual
                                      }));
                                      showNotification(`حالت ممیزی به «${mode.label}» تغییر یافت. دبل‌پس فعال شد.`, "success");
                                    }}
                                    className={`p-2.5 rounded-2xl border text-center transition-all duration-200 flex flex-col justify-between h-20 cursor-pointer ${
                                      isSel
                                        ? isDarkMode
                                          ? "bg-indigo-600/10 border-indigo-500 text-indigo-400 font-bold"
                                          : "bg-indigo-50 border-indigo-500 text-indigo-700 font-bold"
                                        : isDarkMode
                                          ? "bg-slate-900/40 border-slate-800 text-slate-400 hover:bg-slate-800/40"
                                          : "bg-slate-50 border-slate-200 text-slate-600 hover:bg-slate-100"
                                    }`}
                                  >
                                    <div>
                                      <span className="text-[10px] block leading-tight font-black">{mode.label}</span>
                                      <span className="text-[8px] opacity-80 block mt-1 leading-tight">{mode.desc}</span>
                                    </div>
                                    <span className={`text-[7.5px] inline-block px-1 py-0.5 rounded-md mt-1 self-center ${
                                      isSel 
                                        ? isDarkMode ? "bg-indigo-500/20 text-indigo-300" : "bg-indigo-100 text-indigo-800 font-black"
                                        : "bg-slate-200/50 dark:bg-slate-800 text-slate-500"
                                    }`}>
                                      {mode.badge}
                                    </span>
                                  </button>
                                );
                              })}
                            </div>
                          </div>

                          {/* Target Extraction Scopes */}
                          <div className="flex flex-col gap-2">
                            <label className={`text-[11.5px] font-black flex items-center gap-1.5 ${isDarkMode ? "text-slate-200" : "text-slate-800"}`}>
                              <CheckSquare className="w-4 h-4 text-emerald-500" />
                              ۴. اطلاعات مورد نیاز برای استخراج (فیلترهای فعال):
                            </label>
                            <p className={`text-[9.5px] leading-relaxed ${isDarkMode ? "text-slate-400" : "text-slate-500"}`}>
                              تیک مواردی که می‌خواهید هوش مصنوعی حتماً در تصویر جستجو کند را فعال نگه دارید:
                            </p>
                            <div className="grid grid-cols-2 gap-2">
                              {[
                                { id: 'items', label: '🧾 جدول اقلام فاکتور', desc: 'شامل کالاها، تعداد، قیمت و تخفیفات جزئی' },
                                { id: 'parties', label: '🏢 اطلاعات خریدار و فروشنده', desc: 'نام‌ها، شناسه ملی، کد اقتصادی و تلفن' },
                                { id: 'tax', label: '💸 مبالغ مالیات و ارزش افزوده', desc: 'محاسبه عوارض، درصد و مبالغ قانونی' },
                                { id: 'currency', label: '💱 نوع ارز و موازنه نهایی', desc: 'تشخیص ریال/تومان/دلار و تراز دوطرفه' }
